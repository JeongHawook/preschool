<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header" >

    </attr>
<!--
<attr sel="main" th:object="${jsonObj}">

    <attr sel="#newsForm" th:action="@{/news}" th:method="get" />
    <attr sel=".tables" th:each="jsonObj : ${jsonObj.items}">
    <attr sel="#title" th:text="${jsonObj.title}" />
    </attr>
</attr>

-->

</thlogic>